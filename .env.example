# YouTube to Article Converter - Configuration Example
# Copy this file to .env and fill in your actual values

# Hugging Face API Token (required)
HF_TOKEN=your_huggingface_api_token_here

# Whisper Configuration
WHISPER_MODEL=base  # Options: tiny, small, base, medium, large
WHISPER_DEVICE=cuda  # Options: cuda, cpu
WHISPER_COMPUTE_TYPE=float16  # Options: float32, float16, int8

# LLM Models (via Hugging Face Inference)
ANALYZER_MODEL=meta-llama/Llama-3.3-70B-Instruct
WRITER_MODEL=meta-llama/Llama-3.3-70B-Instruct
SEO_MODEL=meta-llama/Llama-3.1-8B-Instruct

# Output Configuration
OUTPUT_DIR=./output
LOG_LEVEL=INFO  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

# Retry Configuration
MAX_RETRIES=3
RETRY_DELAY=5

# Rate Limiting (HuggingFace API requests per minute)
REQUESTS_PER_MINUTE=30

# FFmpeg Path (optional - auto-detect if not set)
# Examples:
# FFMPEG_PATH=/usr/bin  # Linux
# FFMPEG_PATH=C:/ffmpeg/bin  # Windows
# Leave empty to let yt-dlp auto-detect
FFMPEG_PATH=

# Bot Detection Prevention Settings
# These are automatically applied in the code:
# - Rotating User-Agents (5 different browser user agents)
# - Random request delays (1-3 seconds between requests)
# - Realistic HTTP headers (Accept, Accept-Language, etc.)
# - Alternating YouTube player clients (web, android)
# - Proper SSL/TLS verification
# These help prevent YouTube from detecting your requests as bot traffic
